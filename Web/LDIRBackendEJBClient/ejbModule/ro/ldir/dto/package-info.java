/**
 * Provides the data transfer objects used in this application.
 * 
 * The objects in this packages are both EntityBeans (to be persisted in the database), but also
 * serializable using JSON or XML to be exported / used by the webservices.
 *
 */
package ro.ldir.dto;